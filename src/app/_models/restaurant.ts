import { ClassDirective } from '@angular/flex-layout';

export class Restaurant {
    restaurant_id: number;
    name: string;
    address: string;
    phone: string;
    category: string;
    rating: number;
  }